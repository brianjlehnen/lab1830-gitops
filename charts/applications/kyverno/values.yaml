kyverno:
  admissionController:
    replicas: 1
  backgroundController:
    replicas: 1
  cleanupController:
    replicas: 1
  reportsController:
    replicas: 1
  cleanupJobs:
    admissionReports:
      image:
        registry: docker.io
        repository: bitnami/kubectl
        tag: "1.31"
    clusterAdmissionReports:
      image:
        registry: docker.io
        repository: bitnami/kubectl
        tag: "1.31"

policy-reporter:
  ui:
    enabled: true
    ingress:
      enabled: true
      className: nginx
      hosts:
        - host: kyverno-staging.lab1830.com
          paths:
            - path: /
              pathType: Prefix
  kyvernoPlugin:
    enabled: true
  metrics:
    enabled: true
